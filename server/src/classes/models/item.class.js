class Item {
  constructor(item_id, item_name, item_equip_slot, item_hp, item_speed, item_attack, item_price) {
    this.itemId = item_id;
    this.itemName = item_name;
    this.itemEquipSlot = item_equip_slot;
    this.itemHp = item_hp;
    this.itemSpeed = item_speed;
    this.itemAttack = item_attack;
    this.itemPrice = item_price;
  }
}

export default Item;
