syntax = 'proto3';

package common;

message Packet {
    int32 handlerId = 1;
    string userId = 2;
    string clientVersion = 3;
    int32 sequence = 4;
    bytes payload = 5;
}

message Ping {
    int64 timestamp = 1;
}